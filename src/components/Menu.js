import styled from 'styled-components';

export default function Menu() {
  return <MenuContainer>ee</MenuContainer>;
}

const MenuContainer = styled.div`
  display: flex;
  justify-content: space-between;
  align-items: center;

  position: fixed;
  bottom: 0;

  width: 100%;
  height: 70px;

  padding: 10px;

  background: #FFFFFF;
  box-shadow: 0px 4px 4px 0px #00000026;
`;
